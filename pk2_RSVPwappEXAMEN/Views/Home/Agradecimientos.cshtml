|   @model pk2_RSVPwapp.Models.GuestResponse

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Agradecimientos</title>
    <link rel="stylesheet" type="text/css" href="~/Content/Site.css" />
</head>
<body>

    @{
        try{
            WebMail.SmtpServer = "smtp.gmail.com";
            WebMail.SmtpPort = 587;
            WebMail.EnableSsl = true;
            WebMail.UserName = "carlosalfonso.hl@gmail.com";
            WebMail.Password = "chesscarlos";
            WebMail.From = "carlosalfonso.hl@gmail.com";

            WebMail.Send("carlosalfonso.hl@gmail.com", "Notificacion RSVP", Model.Name + ((Model.WillAttend ?? false) ? " SI " : " NO ") + " Asistirá");
            }
        catch(Exception){
                        @:<br>Error: No se pudo enviar el correo</br>
                        }
    }

    
    <div>
        <h1> Gracias, @Model.Name!</h1>
        @if (Model.WillAttend == true)
        {
            @:Que bueno que vendras, las chelas estaran listas en el refri
            
        }
        else
        {
            @: Siento mucho que no puedas venir... :(
        }
         <p>@Html.ActionLink("Regresar", "RsvpForm");</p>
    </div>
</body>
</html>
